#include<stdio.h>
int sort(int *arr,int lenght);
int main(void)
{
    int n,number[n];
    int average,sum=0;
    char *name[8];
    int right;
    for(int i=0;i<n;i++)
    {
        scanf("%s %d",name,&number[i]);
        sum+=number[i];
    }
    average=sum/n;
    for(int i=0;i<n;i++){
        right=average-number[i];
        
        
    }


}